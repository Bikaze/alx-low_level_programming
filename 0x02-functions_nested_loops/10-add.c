/**
  *add - program function to add two numbers
  *@n: will be holding the first number
  *@k: will be holding the second number
  *Return: will return the sum
  */
int add(int n, int k)
{
	return (n + k);
}
