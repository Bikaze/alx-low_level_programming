/**
  *mul - program function to multiply two numbers
  *@a: will be holding the first number
  *@b: will be holding the second number
  *Return: will return the product
  */
int mul(int a, int b)
{
	return (a * b);
}
